\section{Virtual Machine Invocations}\label{sec:virtualmachineinvocations}

We now define the three practical instances where we wish to invoke a \textsc{pvm} instance as part of the protocol. In general, we avoid introducing unbounded data as part of the basic invocation arguments in order to minimize the chance of an unexpectedly large \textsc{ram} allocation, which could lead to gas inflation and unavoidable underflow. This makes for a more cumbersome interface, but one which is more predictable and easier to reason about.

\subsection{Host-Call Result Constants}

\begin{description}
  \item[$\mathtt{NONE} = 2^{64} - 1$] The return value indicating an item does not exist.
  \item[$\mathtt{WHAT} = 2^{64} - 2$] Name unknown.
  \item[$\mathtt{OOB} = 2^{64} - 3$] The inner \textsc{pvm} memory index provided for reading/writing is not accessible.
  \item[$\mathtt{WHO} = 2^{64} - 4$] Index unknown.
  \item[$\mathtt{FULL} = 2^{64} - 5$] Storage full or resource already allocated.
  \item[$\mathtt{CORE} = 2^{64} - 6$] Core index unknown.
  \item[$\mathtt{CASH} = 2^{64} - 7$] Insufficient funds.
  \item[$\mathtt{LOW} = 2^{64} - 8$] Gas limit too low.
  \item[$\mathtt{HUH} = 2^{64} - 9$] The operation is invalid. For example, the item is already solicited, cannot be forgotten, or the service is insufficiently privileged.
  \item[$\mathtt{OK} = 0$] The return value indicating general success.
\end{description}

Inner \textsc{pvm} invocations have their own set of result codes:
\begin{description}
  \item[$\mathtt{HALT} = 0$] The invocation completed and halted normally.
  \item[$\mathtt{PANIC} = 1$] The invocation completed with a panic.
  \item[$\mathtt{FAULT} = 2$] The invocation completed with a page fault.
  \item[$\mathtt{HOST} = 3$] The invocation completed with a host-call fault.
  \item[$\mathtt{OOG} = 4$] The invocation completed by running out of gas.
\end{description}

Note return codes for a host-call-request exit are any non-zero value less than $2^{64} - 13$.

\subsection{Is-Authorized Invocation}\label{sec:isauthorizedinvocation}

The Is-Authorized invocation is the first and simplest of the three, being totally stateless. It provides only host-call functions for inspecting its environment and parameters. It accepts as arguments only the work-package $\wpX$ and the core on which it should be executed, $c$. Formally, it is defined as $\Psi_I$:
\begin{align}
  \label{eq:isauthinvocation}
  \Psi_I &\colon \abracegroup{
    \tuple{\workpackage, \coreindex} &\to \tuple{\blob \cup \workerror, \gas} \\
    \tup{\wpX, c} &\mapsto \begin{cases}
      \tup{\token{BAD}, 0} &\when \wpX_\wp¬authcode = \none \\
      \tup{\token{BIG}, 0} &\otherwhen \len{\wpX_\wp¬authcode} > \Cmaxauthcodesize \\
      \tup{\mathbf{r}, u} &\otherwise \\
      \multicolumn{2}{l}{\where \tup{u, \mathbf{r}, \none} = \Psi_M(\wpX_\wp¬authcode, 0, \Cpackageauthgas, \encode[2]{c}, F, \none)}\\
    \end{cases}\\
  } \\
  \label{eq:isauthorizedmutator}F \in \contextmutator{\set{\none}} &\colon
    \tup{n, \gascounter, \registers, \memory, \none} \mapsto \begin{cases}
      \Omega_G(\gascounter, \registers, \memory, \none) &\when n = \mathtt{gas} \\
      \Omega_\Gemini(\gascounter, \registers, \memory, \none, \wpX_\wp¬authcode) &\when n = \mathtt{grow\_heap} \\
      \Omega_Y(\gascounter, \registers, \memory, \none, \wpX, \none, \none, \none, \none, \none, \none) &\when n = \mathtt{fetch} \\
      \tup{\oog, 0, \registers', \memory, \none} &\otherwhen \gascounter < \Cgasunknown \\
      \tup{\continue, \gascounter - \Cgasunknown, \registers', \memory, \none} &\otherwise \\
      \multicolumn{2}{l}{\where \registers' = \registers \exc \registers'_7 = \mathtt{WHAT}}
    \end{cases}
\end{align}

\subsection{Refine Invocation}\label{sec:refineinvocation}

We define the Refine service-account invocation function as $\Psi_R$. It has no general access to the state of the \Jam chain, with the slight exception being the ability to make a historical lookup. Beyond this it is able to create inner instances of the \textsc{pvm} and dictate pieces of data to export.

The historical-lookup host-call function, $\Omega_H$, is designed to give the same result regardless of the state of the chain for any time when auditing may occur (which we bound to be less than two epochs from being accumulated). The lookup anchor may be up to $\Cmaxlookupanchorage$ timeslots before the recent history and therefore adds to the potential age at the time of audit. We therefore set $\Cexpungeperiod$ to have a safety margin of eight hours:
\begin{equation}
  \Cexpungeperiod \equiv \Cmaxlookupanchorage + 4,800 = 19,200
\end{equation}


The inner \textsc{pvm} invocation host-calls, meanwhile, depend on an integrated \textsc{pvm} type, which we shall denote $\pvm¬instance$. It holds a \textsc{pvm} program blob, instruction counter, \textsc{ram}, a flag denoting whether gas was already charged for the currently executing basic block, and the handle of the compiled program module from which it was instantiated:
\begin{equation}\label{eq:innerpvm}
  \pvm¬instance \equiv \tuple{\isa{\pg¬code}{\blob}, \isa{\pg¬ram}{\ram}, \isa{\pg¬pc}{\pvmreg}, \isa{\pg¬gaschargedflag}{\bool}, \isa{\pg¬handle}{\N}}
\end{equation}

The Export host-call depends on two pieces of context; one sequence of segments (blobs of length $\Csegmentsize$) to which it may append, and the other an argument passed to the invocation function to dictate the number of segments prior which may be assumed to have already been appended. The latter value ensures that an accurate segment index can be provided to the caller.

Unlike the other invocation functions, the Refine invocation function implicitly draws upon some recent service account state item $\accounts$. The specific block from which this comes is not important, as long as it is no earlier than its work-package's lookup-anchor block. It explicitly accepts the work-package $p$ and the index of the work item to be refined, $i$ together with the core which is doing the refining $c$. Additionally, the authorizer trace $\mathbf{r}$ is provided together with all work items' import segments $\overline{\mathbf{i}}$ and an export segment offset $\segoff$. It results in a tuple of some error $\workerror$ or the refinement output blob (signalling success), the export sequence in the case of success and the gas used in evaluation. Formally:
\begin{align}
  &\Psi_R \colon \abracegroup{
    \label{eq:refinvocation}
    \tuple{\coreindex, \N, \workpackage, \blob, \sequence{\sequence{\segment}}, \N} &\to \tuple{\blob \cup \workerror, \sequence{\segment}, \gas} \\
    \tup{c, i, p, \mathbf{r}, \overline{\mathbf{i}}, \segoff} &\mapsto \begin{cases}
      \tup{\token{BAD}, \sq{}, 0} &\when w_\wi¬serviceindex \not\in \keys{\accounts} \vee \histlookup(\accounts\subb{w_\wi¬serviceindex}, (p_\wp¬context)_\wc¬lookupanchortime, w_\wi¬codehash) = \none \\
      \tup{\token{BIG}, \sq{}, 0} &\otherwhen \len{\histlookup(\accounts\subb{w_\wi¬serviceindex}, (p_\wp¬context)_\wc¬lookupanchortime, w_\wi¬codehash)} > \Cmaxservicecodesize \\
      &\otherwise: \\
      &\quad\using \mathbf{a} = \encode{c, i, w_\wi¬serviceindex, \var{w_\wi¬payload}, \blake{p}}\;,\ \encode{\var{\mathbf{z}}, \jam¬blob} = \histlookup(\accounts\subb{w_\wi¬serviceindex}, (p_\wp¬context)_\wc¬lookupanchortime, w_\wi¬codehash)\\
      &\quad\also \tup{u, \mathbf{o}, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}} = \Psi_M(\jam¬blob, 0, w_\wi¬refgaslimit, \mathbf{a}, F, \tup{\emptyset, \emptyset, \sq{}})\ \colon\\
      \tup{\mathbf{o}, \sq{}, u} &\quad\when \mathbf{o} \in \set{ \oog, \panic }  \\
      \tup{\mathbf{o}, \mathbf{e}, u} &\quad\otherwise \\
      \multicolumn{2}{l}{\where w = p_\wp¬workitems\subb{i}}
    \end{cases} \\
  } \\
  \label{eq:refinemutator}
  &F \in \contextmutator{\tuple{\dictionary{\N}{\pvm¬instance}, \dictionary{\N}{\pvm¬module}, \sequence{\segment}}} \colon
    (n, \gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) \mapsto \begin{cases}
      \Omega_G(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{gas} \\
      \Omega_\Gemini(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, \jam¬blob) &\when n = \mathtt{grow\_heap} \\
      \Omega_Y(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, p, \zerohash, \mathbf{r}, i, \overline{\mathbf{i}}, \overline{\mathbf{x}}, \none) &\when n = \mathtt{fetch}\\
      \Omega_H(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, w_\wi¬serviceindex, \accounts, (p_\wp¬context)_\wc¬lookupanchortime) &\when n = \mathtt{historical\_lookup}\\
      \Omega_E(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, \segoff) &\when n = \mathtt{export}\\
      \Omega_\Gamma(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, w_\wi¬serviceindex, \accounts, (p_\wp¬context)_\wc¬lookupanchortime) &\when n = \mathtt{compile}\\
      \Omega_M(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{machine}\\
      \Omega_P(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{peek}\\
      \Omega_O(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{poke}\\
      \Omega_Z(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{pages}\\
      \Omega_K(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{invoke}\\
      \Omega_X(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}) &\when n = \mathtt{expunge}\\
      \tup{\oog, 0, \registers', \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}} &\otherwhen \gascounter < \Cgasunknown\\
      \tup{\continue, \gascounter - \Cgasunknown, \registers', \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}} &\otherwise\\
      \multicolumn{2}{l}{\where \registers' = \registers \exc \registers'_7 = \mathtt{WHAT}} \\
      \multicolumn{2}{l}{\also \overline{\mathbf{x}} = \sq{\build{
        \sq{\build{
          \mathbf{x}
        }{
          \tup{\blake{\mathbf{x}}, \len{\mathbf{x}}} \orderedin \wiX_\wi¬extrinsics
        }}
      }{
        \wiX \orderedin p_\wp¬workitems
      }}}
    \end{cases}
\end{align}

\subsection{Accumulate Invocation}\label{sec:accumulateinvocation}

Since this is a transition which can directly affect a substantial amount of on-chain state, our invocation context is accordingly complex. It is a tuple with elements for each of the aspects of state which can be altered through this invocation and beyond the account of the service itself includes the deferred transfer list and several dictionaries for alterations to preimage lookup state, core assignments, validator key assignments, newly created accounts and alterations to account privilege levels.

Formally, we define our result context to be $\implications$, and our invocation context to be a pair of these contexts, $\implications \times \implications$ (and thus for any value $\imX \in \implications$ there exists $\imX^2 \in \implications \times \implications$), with one dimension being the regular dimension and generally named $\imX$ and the other being the exceptional dimension and being named $\imY$. The only function which actually alters this second dimension is $\mathtt{checkpoint}$, $\Omega_C$ and so it is rarely seen.
\begin{align}\label{eq:implications}
  \implications &\equiv \tuple{
    \isa{\im¬id}{\serviceid},
    \isa{\im¬state}{\partialstate},
    \isa{\im¬modifiedaccounts}{\protoset{\serviceid}},
    \isa{\im¬nextfreeid}{\serviceid},
    \isa{\im¬xfers}{\defxfers},
    \isa{\im¬yield}{\optional{\hash}},
    \isa{\im¬provisions}{\protoset{\tuple{\serviceid, \blob}}}
  }\\
  \forall \imX \in \implications :
    \imX_\im¬self &\equiv (\imX_\im¬state)_\ps¬accounts\subb{\imX_\im¬id}
\end{align}

We define a convenience equivalence $\imX_\im¬self$ to easily denote the accumulating service account.

We track both regular and exceptional dimensions within our context mutator, but collapse the result of the invocation to one or the other depending on whether the termination was regular or exceptional (\ie out-of-gas or panic).

We define $\Psi_A$, the Accumulation invocation function as:
\newcommand*{\local¬basestate}{\ao¬poststate}
\newcommand*{\local¬postxferstate}{\mathbf{s}}
\begin{gather}
  \label{eq:accinvocation}
  \Psi_A \colon\abracegroup{
    \tuple{
      \partialstate, \timeslot, \serviceid, \gas, \sequence{\operandtuple \cup \defxfer}
    }
    &\to \acconeout \\
    \tup{\local¬basestate, t, s, g, \mathbf{i}} &\mapsto \begin{cases}
      \tup{
        \ao¬poststate,
        \is{\ao¬modifiedaccounts}{\emset},
        \is{\ao¬defxfers}{\sq{}},
        \is{\ao¬yield}{\none},
        \is{\ao¬gasused}{0},
        \is{\ao¬provisions}{\sq{}}
      }
        &\when s \not\in \keys{\local¬basestate_\ps¬accounts} \\
      \tup{
        \is{\ao¬poststate}{\local¬postxferstate},
        \ao¬modifiedaccounts,
        \is{\ao¬defxfers}{\sq{}},
        \is{\ao¬yield}{\none},
        \is{\ao¬gasused}{0},
        \is{\ao¬provisions}{\sq{}}
      }
        &\otherwhen \jam¬blob = \none \vee \len{\jam¬blob} > \Cmaxservicecodesize \\
      C(\Psi_M(\jam¬blob, 5, g, \encode{t, s, \len{\mathbf{i}}}, F, I(\local¬postxferstate, \ao¬modifiedaccounts, s)^2))
        &\otherwise
    \end{cases} \\
    &\phantom{{}\mapsto{}} \where \jam¬blob = \local¬basestate_\ps¬accounts\subb{s}_\sa¬code \\
    &\phantom{{}\mapsto{}} \also \local¬postxferstate = \local¬basestate \exc \abracegroup{
      \local¬postxferstate_\ps¬accounts\subb{s}_\sa¬balance &= \local¬basestate_\ps¬accounts\subb{s}_\sa¬balance + \!\!\!\!\!\sum_{r \in \mathbf{x}, r_\dx¬destsupervisor = \bot}\!\!\!\!\!r_\dx¬amount \\
      \local¬postxferstate_\ps¬accounts\subb{s}_\sa¬supervisorbalance &= \local¬basestate_\ps¬accounts\subb{s}_\sa¬supervisorbalance + \!\!\!\!\!\sum_{r \in \mathbf{x}, r_\dx¬destsupervisor = \top}\!\!\!\!\!r_\dx¬amount
    } \\
    &\phantom{{}\mapsto{}} \also \ao¬modifiedaccounts = \set{\build{s}{r \in \mathbf{x}}} \\
    &\phantom{{}\mapsto{}} \also \mathbf{x} = \sq{\build{i}{
      i \orderedin \mathbf{i} ,
      i \in \defxfer
    }}
  }\\
  I\colon\abracegroup{
    \tuple{\partialstate, \protoset{\serviceid}, \serviceid} &\to \implications\\
    \tup{\im¬state, \im¬modifiedaccounts, \im¬id} &\mapsto \tup{
      \im¬id,
      \im¬state,
      \im¬modifiedaccounts,
      \im¬nextfreeid,
      \is{\im¬xfers}{\sq{}},
      \is{\im¬yield}{\none},
      \is{\im¬provisions}{\sq{}}
    }\\
    &\qquad\where \im¬nextfreeid = \text{check}((\decode[4]{\blake{\encode{\im¬id, \entropyaccumulator', \H_\¬timeslot}}} \bmod (2^{32}-\Cminpublicindex-2^8)) + \Cminpublicindex) \\
  }\\
  F \in \contextmutator{\implicationspair} \colon \tup{n, \gascounter, \registers, \memory, \imXY} \mapsto \begin{cases}
    \Omega_G(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{gas} \\
    \Omega_\Gemini(\gascounter, \registers, \memory, \imXY, \jam¬blob) &\when n = \mathtt{grow\_heap} \\
    \Omega_Y(\gascounter, \registers, \memory, \imXY, \none, \entropyaccumulator', \none, \none, \none, \none, \mathbf{i}) &\when n = \mathtt{fetch}\\
    \Omega_R(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{read} \\
    \Omega_W(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{write} \\
    \Omega_L(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{lookup} \\
    \Omega_I(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{info} \\
    \Omega_B(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{bless}\\
    \Omega_A(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{assign}\\
    \Omega_D(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{designate}\\
    \Omega_C(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{checkpoint} \\
    \Omega_N(\gascounter, \registers, \memory, \imXY, \H_\¬timeslot) &\when n = \mathtt{new} \\
    \Omega_V(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{supervisor} \\
    \Omega_U(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{upgrade} \\
    \Omega_T(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{transfer} \\
    \Omega_J(\gascounter, \registers, \memory, \imXY, \H_\¬timeslot) &\when n = \mathtt{eject} \\
    \Omega_Q(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{query} \\
    \Omega_S(\gascounter, \registers, \memory, \imXY, \H_\¬timeslot) &\when n = \mathtt{solicit} \\
    \Omega_F(\gascounter, \registers, \memory, \imXY, \H_\¬timeslot) &\when n = \mathtt{forget} \\
    \Omega_\Taurus(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{yield} \\
    \Omega_\Aries(\gascounter, \registers, \memory, \imXY) &\when n = \mathtt{provide} \\
    \tup{\oog, 0, \registers', \memory, \imXY} &\otherwhen \gascounter < \Cgasunknown\\
    \tup{\continue, \gascounter - \Cgasunknown, \registers', \memory, \imXY} &\otherwise\\
    \multicolumn{2}{l}{\where \registers' = \registers \exc \registers'_7 = \mathtt{WHAT}}
  \end{cases} \\
  C\colon\abracegroup{
    \tuple{\gas, \blob \cup \set{\oog, \panic}, \implicationspair} &\to \acconeout \\
    \tup{\ao¬gasused, \mathbf{o}, \imXY} &\mapsto \begin{cases}
      \tup{
        \is{\ao¬poststate}{\imY_\im¬state},
        \is{\ao¬modifiedaccounts}{\imY_\im¬modifiedaccounts},
        \is{\ao¬defxfers}{\imY_\im¬xfers},
        \is{\ao¬yield}{\imY_\im¬yield},
        \ao¬gasused,
        \is{\ao¬provisions}{\imY_\im¬provisions}
      } & \when \mathbf{o} \in \set{\oog, \panic} \\
      \tup{
        \is{\ao¬poststate}{\imX_\im¬state},
        \is{\ao¬modifiedaccounts}{\imX_\im¬modifiedaccounts},
        \is{\ao¬defxfers}{\imX_\im¬xfers},
        \is{\ao¬yield}{\mathbf{o}},
        \ao¬gasused,
        \is{\ao¬provisions}{\imX_\im¬provisions}
      } & \otherwhen \mathbf{o} \in \hash \\
      \tup{
        \is{\ao¬poststate}{\imX_\im¬state},
        \is{\ao¬modifiedaccounts}{\imX_\im¬modifiedaccounts},
        \is{\ao¬defxfers}{\imX_\im¬xfers},
        \is{\ao¬yield}{\imX_\im¬yield},
        \ao¬gasused,
        \is{\ao¬provisions}{\imX_\im¬provisions}
      } & \otherwise \\
    \end{cases}
  }
\end{gather}

The mutator $F$ governs how this context will alter for any given parameterization, and the collapse function $C$ selects one of the two dimensions of context depending on whether the virtual machine's halt was regular or exceptional.

The initializer function $I$ maps some partial state, a set identifying modified services, and a service account index to yield a mutator context such that no further alterations to the given state are implied in either exit scenario. Note that the component $\im¬nextfreeid$ utilizes the random accumulator $\entropyaccumulator'$ and the block's timeslot $\H_\¬timeslot$ to create a deterministic sequence of identifiers which are extremely likely to be unique.

Concretely, we create the identifier from the Blake2 hash of the identifier of the creating service, the current random accumulator $\entropyaccumulator'$ and the block's timeslot. Thus, within a service's accumulation it is almost certainly unique, but it is not necessarily unique across all services, nor at all times in the past. We utilize a \emph{check} function to find the first such index in this sequence which does not already represent a service:
\begin{equation}
  \label{eq:newserviceindex}
  \text{check}(i \in \serviceid) \equiv \begin{cases}
    i &\when i \not\in \keys{\local¬basestate_\ps¬accounts} \\
    \text{check}((i - \Cminpublicindex + 1) \bmod (2^{32}-2^8-\Cminpublicindex) + \Cminpublicindex)&\otherwise
  \end{cases}
\end{equation}

\nb In the highly unlikely event that a block executes to find that a single service index has inadvertently been attached to two different services, then the block is considered invalid. Since no service can predict the identifier sequence ahead of time, they cannot intentionally disadvantage the block author.

\undef{\local¬basestate}







\subsection{Host Functions}

We come now to defining the host functions which are utilized by the \textsc{pvm} invocations. These map some \textsc{pvm} state, including invocation context $\context$, possibly together with some additional parameters, to a new \textsc{pvm} state.

All host functions are of the form $\tup{\execst^* \in \set{\continue, \panic, \oog}, \gascounter^* \in \gas, \registers^* \in \regs, \memory^* \in \ram, \context^*} = \Omega_\square(\gascounter \in \gas, \registers \in \regs, \memory \in \ram, \context, \dots)$. The gas cost of a host-call is given by $g$, defined in the tables below; should there be insufficient gas to cover this cost, $\execst^* = \oog$ is returned, all remaining gas is consumed, and registers, memory and context are left unchanged. Formally:
\begin{align}
  \gascounter^* &\equiv \max(\gascounter - g, 0)\quad\text{unless $\gascounter^*$ is explicitly defined below} \\
  \tup{\execst^*, \registers^*, \memory^*, \context^*} &\equiv \begin{cases}
    \tup{\oog, \registers, \memory, \context} &\when \gascounter < g \\
    \tup{\execst', \registers', \memory', \context'} &\otherwise
  \end{cases}
\end{align}

Memory-sized gas for host-calls is given by a rate $L$ (gas per 1024 octets) and size $\ell$ (octets). The following defines the gas cost for such a size:
\begin{equation}\label{eq:fnmemgas}
  \fnmemgas(L, \ell) \equiv \ceil{\frac{L \cdot \ell}{1024}}
\end{equation}
With base cost $c$, total gas is $c + \fnmemgas(L, \ell)$. The host-call tables below use this formula for all memory-sized terms.

When transitioning \textsc{pvm} state for a host-call, a number of conditions typically hold true and host functions are defined essentially by their exceptions to these rules. Specifically, the machine does not halt, and \textsc{ram}, registers and context are unchanged. Formally:
\begin{equation}
  \execst' \equiv \continue,\quad \registers' \equiv \registers,\quad \memory' \equiv \memory,\quad \context' \equiv \context\qquad\text{except as indicated below}
\end{equation}

\subsubsection{General Functions}\label{sec:generalfunctions}

These make no assumptions about the context $\context$; it is always passed through untouched, \ie $\context' = \context$.

\aboverulesep = 1.5mm \belowrulesep = 2mm

\begin{longtable}{p{4cm} p{12cm}}
  \toprule
  \thead*{\textbf{Function} \\ \textbf{Identifier}} &
  \thead{\textbf{Mutations} \\ \textbf{Gas usage (}$g$\textbf{)}} \\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \endhead
  \makecell*[l]{
  $\Omega_G(\gascounter, \registers, \memory, \context)$ \\
  \texttt{gas} = 0} &
  $\begin{aligned}
    \registers'_7 &\equiv \gascounter^* \\
    g &\equiv \CgasG
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_\Gemini(\gascounter, \registers, \memory, \context, \jam¬blob)$ \\
  \texttt{grow\_heap} = 1} &
  $\begin{aligned}
    % This hostcall can be used to expand the RW data region of memory, or to query its size.
    %
    % It takes a single argument: the index of last writable page (plus one) the user would like
    % to be able to access within the RW data region of the address space.
    %
    % It returns the index (plus one) of the last writable page in the RW data region, regardless
    % of whether anything has changed.
    %
    % Extract the RO data size and the stack size from the JAM program blob.
    \using (\mathbf{o}, s) &\mapsto \jam¬blob \text{ according to eq. \ref{eq:conditions}} \\
    % Page index of the start of the RW data region.
    \using a &= \frac{2\Cpvminitzonesize + \rnq{\len{\mathbf{o}}}}{\Cpvmpagesize} \\
    % Page index plus one of the last possible RW data region page.
    \using b &= \frac{2^{32} - 3\Cpvminitzonesize - \Cpvminitinputsize - \rnq{s}}{\Cpvmpagesize} \\
    % Current writable page count of the RW data region.
    \using c &= |\set{\build{p \in \Nclamp{a}{b}}{\memory_\ram¬access\subb{p} = \mathrm{W}}}| \\
    % Current heap pointer.
    \using h &= a + c \\
    \registers'_7 &\equiv \begin{cases}
      % Do nothing if the heap is large enough already, or if the requested size was too large.
      h &\when \registers_7 \leq h \lor \registers_7 > b \\
      % Expand the accessible RW data region.
      \registers_7 &\otherwise
    \end{cases}\\
    (\memory'_\ram¬access)\interval{a}{\registers_7'} &\equiv \sq{\mathrm{W}, \dots, \mathrm{W}} \\
    g &\equiv \CgasGeminiconst + (\registers_7' - h) \cdot \CgasGeminilinear
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_Y(\gascounter, \registers, \memory, \context, p, n, \mathbf{r}, i, \overline{\mathbf{i}}, \overline{\mathbf{x}}, \mathbf{i})$ \\
  \texttt{fetch} = 2} &
  $\begin{aligned}
    \using \mathbf{v} &= \begin{cases}
      \mathbf{c} &\when \registers_{10} = 0 \\
      \multicolumn{2}{l}{\where \mathbf{c} = \encode{
        \begin{aligned}
          &\encode[8]{\Citemdeposit},
          \encode[8]{\Cbytedeposit},
          \encode[8]{\Cbasedeposit},
          \encode[2]{\Ccorecount},
          \encode[4]{\Cexpungeperiod},
          \encode[4]{\Cepochlen},
          \encode[8]{\Creportaccgas},\\
          &\encode[8]{\Cpackageauthgas},
          \encode[8]{\Cpackagerefgas},
          \encode[8]{\Cblockaccgas},
          \encode[2]{\Crecenthistorylen},
          \encode[2]{\Cmaxpackageitems},
          \encode[2]{\Cmaxreportdeps},
          \encode[2]{\Cmaxblocktickets},\\
          &\encode[4]{\Cmaxlookupanchorage},
          \encode[2]{\Cauthpoolsize},
          \encode[2]{\Cslotseconds},
          \encode[2]{\Cauthqueuesize},
          \encode[2]{\Crotationperiod},
          \encode[2]{\Cmaxpackagexts},
          \encode[2]{\Cassurancetimeoutperiod},\\
          &\encode[4]{\Cmaxauthcodesize},
          \encode[4]{\Cmaxbundlesize},
          \encode[4]{\Cmaxservicecodesize},
          \encode[4]{\Cmaxpackageimports},
          \encode[4]{\Cmaxreportvarsize},\\
          &\encode[4]{\Cmemosize},
          \encode[4]{\Cmaxpackageexports},
          \encode[4]{\Cepochtailstart}
        \end{aligned}
      }}\\
      n &\when n \ne \none \wedge \registers_{10} = 1 \\
      \mathbf{r} &\when \mathbf{r} \ne \none \wedge \registers_{10} = 2 \\
      \overline{\mathbf{x}}[\registers_{11}]_{\registers_{12}} &\when \overline{\mathbf{x}} \ne \none \wedge \registers_{10} = 3 \wedge \registers_{11} < \len{\overline{\mathbf{x}}} \wedge \registers_{12} < \len{\overline{\mathbf{x}}[\registers_{11}]} \\
      \overline{\mathbf{x}}\subb{i}_{\registers_{11}} &\when \overline{\mathbf{x}} \ne \none \wedge i \ne \none \wedge \registers_{10} = 4 \wedge \registers_{11} < \len{\overline{\mathbf{x}}\subb{i}} \\
      \overline{\mathbf{i}}[\registers_{11}]_{\registers_{12}} &\when \overline{\mathbf{i}} \ne \none \wedge \registers_{10} = 5 \wedge \registers_{11} < \len{\overline{\mathbf{i}}} \wedge \registers_{12} < \len{\overline{\mathbf{i}}[\registers_{11}]} \\
      \overline{\mathbf{i}}\subb{i}_{\registers_{11}} &\when \overline{\mathbf{i}} \ne \none \wedge i \ne \none \wedge \registers_{10} = 6 \wedge \registers_{11} < \len{\overline{\mathbf{i}}\subb{i}} \\
      \encode{p} &\when p \ne \none \wedge \registers_{10} = 7 \\
      p_\wp¬authconfig &\when p \ne \none \wedge \registers_{10} = 8 \\
      p_\wp¬authtoken &\when p \ne \none \wedge \registers_{10} = 9 \\
      \encode{p_\wp¬context} &\when p \ne \none \wedge \registers_{10} = 10 \\
      \encode{\var{\sq{\build{S(w)}{w \orderedin p_\wp¬workitems}}}} &\when p \ne \none \wedge \registers_{10} = 11 \\
      S(p_\wp¬workitems[\registers_{11}]) &\when p \ne \none \wedge \registers_{10} = 12 \wedge \registers_{11} < \len{p_\wp¬workitems} \\
      \multicolumn{2}{l}{\where S(w) \equiv \encode{\encode[4]{w_\wi¬serviceindex}, w_\wi¬codehash, \encode[8]{w_\wi¬refgaslimit, w_\wi¬accgaslimit}, \encode[2]{w_\wi¬exportcount, \len{w_\wi¬importsegments}, \len{w_\wi¬extrinsics}}, \encode[4]{\len{w_\wi¬payload}}}} \\
      p_\wp¬workitems[\registers_{11}]_\wi¬payload &\when p \ne \none \wedge \registers_{10} = 13 \wedge \registers_{11} < \len{p_\wp¬workitems} \\
      \encode{\var{\mathbf{i}}} &\when \mathbf{i} \ne \none \wedge \registers_{10} = 14 \\
      \encode{\mathbf{i}[\registers_{11}]} &\when \mathbf{i} \ne \none \wedge \registers_{10} = 15 \wedge \registers_{11} < \len{\mathbf{i}} \\
      \none &\otherwise
    \end{cases} \\
    \using \sq{o, f_0, z} &= \registers\subrange{7}{3} \\
    \using f &= \min(f_0, \len{\mathbf{v}}) \\
    \using l &= \min(z, \len{\mathbf{v}} - f) \\
    \tup{\execst', \registers'_7, \memory'\subrange{o}{l}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \memory\subrange{o}{l}} &\when \Nrange{o}{l} \not\subseteq \writable{\memory} \\
      \tup{\continue, \mathtt{NONE}, \memory\subrange{o}{l}} &\otherwhen \mathbf{v} = \none \\
      \tup{\continue, \len{\mathbf{v}}, \mathbf{v}\subrange{f}{l}} &\otherwise \\
    \end{cases} \\
    g &\equiv \begin{cases}
      \CgasYc{\registers_{10}} + \fnmemgas(\CgasYl{\registers_{10}}, z) &\when \registers_{10} < 16 \\
      \CgasYunknown &\otherwise
    \end{cases}
  \end{aligned}$\\
  \bottomrule
\end{longtable}

\subsubsection{Refine Functions}\label{sec:refinefunctions}

These assume some refine context triple $\tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}} \in \tuple{\dictionary{\N}{\pvm¬instance}, \dictionary{\N}{\pvm¬module}, \sequence{\segment}}$, which are all initially empty.
\begin{equation}
  \context \equiv \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}\ ,\qquad \context' \equiv \tup{\mathbf{m}', \pvm¬module¬map', \mathbf{e}'}
\end{equation}

\begin{longtable}{p{4cm} p{12cm}}
  \toprule
  \thead*{\textbf{Function} \\ \textbf{Identifier}} &
  \thead{\textbf{Mutations} \\ \textbf{Gas usage (}$g$\textbf{)}} \\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \endhead
  \makecell*[l]{
  $\Omega_H(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, s, \mathbf{d}, t)$ \\
  \texttt{historical\_lookup} = 7} &
  $\begin{aligned}
    \using \mathbf{a} &= \begin{cases}
      \mathbf{d}\subb{s} &\when \registers_7 = 2^{64} - 1 \\
      \mathbf{d}[\registers_7] &\otherwise
    \end{cases} \\
    \using \sq{h, o} &= \registers\subrange{8}{2} \\
    \using z &= \registers_{11} \\
    \using \mathbf{v} &= \begin{cases}
      \error &\when \Nrange{h}{32} \not\subseteq \readable{\memory} \\
      \none &\otherwhen \mathbf{a} = \none \\
      \histlookup(\mathbf{a}, t, \memory\subrange{h}{32}) &\otherwise \\
    \end{cases} \\
    \using f &= \min(\registers_{10}, \len{\mathbf{v}}) \\
    \using l &= \min(z, \len{\mathbf{v}} - f) \\
    \tup{\execst', \registers'_7, \memory'\subrange{o}{l}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \memory\subrange{o}{l}} &\when \mathbf{v} = \error \vee \Nrange{o}{l} \not\subseteq \writable{\memory}\\
      \tup{\continue, \mathtt{NONE}, \memory\subrange{o}{l}} &\otherwhen \mathbf{v} = \none \\
      \tup{\continue, \len{\mathbf{v}}, \mathbf{v}\subrange{f}{l}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasHconst + \fnmemgas(\CgasHlinear, z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_E(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, \segoff)$ \\
  \texttt{export} = 8} &
  $\begin{aligned}
    \using p &= \registers_7 \\
    \using z &= \min(\registers_8, \Csegmentsize) \\
    \using \mathbf{x} &= \begin{cases}
      \zeropad{\Csegmentsize}{\mem\subrange{p}{z}} &\when \Nrange{p}{z} \subseteq \readable{\memory}\\
      \error &\otherwise
    \end{cases}\\
    \tup{\execst', \registers'_7, \mathbf{e}'} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \mathbf{e}} &\when \mathbf{x} = \error \\
      \tup{\continue, \mathtt{FULL}, \mathbf{e}} &\otherwhen \segoff+\len{\mathbf{e}} \ge \Cmaxpackageexports \\
      \tup{\continue, \segoff + \len{\mathbf{e}}, \mathbf{e} \append \mathbf{x}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasE
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_\Gamma(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}}, s, \mathbf{d}, t)$ \\
  \texttt{compile} = 9} &
  $\begin{aligned}
    \using \sq{m, p, l, f, i} &= \registers\subrange{7}{5} \\
    \using \mathbf{a} &= \begin{cases}
      \mathbf{d}\subb{s} &\when i = 2^{64} - 1 \\
      \mathbf{d}\subb{i} &\otherwise
    \end{cases} \\
    \using \mathbf{p} &= \begin{cases}
      \memory\subrange{p}{l} &\when m = 0 \wedge \Nrange{p}{l} \subseteq \readable{\memory} \\
      \error &\otherwhen m = 0 \vee (m = 1 \wedge \Nrange{p}{32} \not\subseteq \readable{\memory}) \\
      \mathbf{h}\subrange{f^*}{l^*} &\otherwhen m = 1 \wedge \mathbf{a} \ne \none \wedge \mathbf{h} \ne \none \\
      \multicolumn{2}{l}{\where \mathbf{h} = \histlookup(\mathbf{a}, t, \memory\subrange{p}{32}),\ f^* = \min(f, \len{\mathbf{h}}),\ l^* = \min(l, \len{\mathbf{h}} - f^*)} \\
      \none &\otherwise
    \end{cases} \\
    \using \mathbf{H} &= \keys{\pvm¬module¬map} \cup \set{\mathbf{m}\subb{n}_\pg¬handle : n \in \keys{\mathbf{m}}} \\
    \using k &= \min(n \in \N : n \not\in \mathbf{H}) \\
    \tup{\execst', \registers'_7, \pvm¬module¬map'} &\equiv \begin{cases}
      \tup{\continue, \mathtt{FULL}, \pvm¬module¬map} &\when \len{\mathbf{H}} \ge 128 \\
      \tup{\panic, \registers_7, \pvm¬module¬map} &\otherwhen \mathbf{p} = \error \\
      \tup{\continue, \mathtt{NONE}, \pvm¬module¬map} &\otherwhen m = 1 \wedge \mathbf{p} = \none \\
      \tup{\continue, \mathtt{HUH}, \pvm¬module¬map} &\otherwhen \mathbf{p} = \none \vee \deblob(\mathbf{p}, 0) = \error \\
      \tup{\continue, k, \pvm¬module¬map \cup \set{\kv{k}{\mathbf{p}}} } &\otherwise \\
    \end{cases} \\
    g &\equiv \begin{cases}
      \CgasGammamemconst + \fnmemgas(\CgasGammamemlinear, l) &\when m = 0 \\
      \CgasGammadbconst + \fnmemgas(\CgasGammadblinear, l) &\when m = 1 \\
      \CgasGammamemconst &\otherwise
    \end{cases}
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_M(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{machine} = 10} &
  $\begin{aligned}
    \using \sq{h, i} &= \registers\subrange{7}{2} \\
    \using n &= \min(n \in \N : n \not\in \keys{\mathbf{m}}) \\
    \using \mathbf{u} &= \tup{\is{\ram¬value}{[0, 0, \dots]},\is{\ram¬access}{[\none, \none, \dots]}} \\
    \tup{\execst', \registers'_7, \mathbf{m}'} &\equiv \begin{cases}
      \tup{\continue, \mathtt{FULL}, \mathbf{m}} &\when \len{\mathbf{m}} \ge 63 \\
      \tup{\continue, \mathtt{WHO}, \mathbf{m}} &\otherwhen h \not\in \keys{\pvm¬module¬map} \\
      \tup{\continue, \mathtt{HUH}, \mathbf{m}} &\otherwhen \deblob(\pvm¬module¬map\subb{h}, i) = \error \\
      \tup{\continue, n, \mathbf{m} \cup \set{\kv{n}{\tup{\pvm¬module¬map\subb{h}, \mathbf{u}, i, \bot, h}}} } &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasM
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_P(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{peek} = 11} &
  $\begin{aligned}
    \using \sq{n, o, s, z} &= \registers\subrange{7}{4} \\
    \tup{\execst', \registers'_7, \mem'} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \mem} &\when \Nrange{o}{z} \not\subseteq \writable{\memory} \\
      \tup{\continue, \mathtt{WHO}, \mem} &\otherwhen n \not\in \keys{\mathbf{m}} \\
      \tup{\continue, \mathtt{OOB}, \mem} &\otherwhen \Nrange{s}{z} \not\subseteq \readable{\mathbf{m}\subb{n}_\pg¬ram} \\
      \tup{\continue, \mathtt{OK}, \mem'} &\otherwise \\
      \multicolumn{2}{l}{\where \mem' = \mem \exc \mem\subrange{o}{z} = (\mathbf{m}\subb{n}_\pg¬ram)\subrange{s}{z}}
    \end{cases} \\
    g &\equiv \CgasPconst + \fnmemgas(\CgasPlinear, z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_O(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{poke} = 12} &
  $\begin{aligned}
    \using \sq{n, s, o, z} &= \registers\subrange{7}{4} \\
    \tup{\execst', \registers'_7, \mathbf{m}'} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \mathbf{m}} &\when \Nrange{s}{z} \not\subseteq \readable{\memory} \\
      \tup{\continue, \mathtt{WHO}, \mathbf{m}} &\otherwhen n \not\in \keys{\mathbf{m}} \\
      \tup{\continue, \mathtt{OOB}, \mathbf{m}} &\otherwhen \Nrange{o}{z} \not\subseteq \writable{\mathbf{m}\subb{n}_\pg¬ram} \\
      \tup{\continue, \mathtt{OK}, \mathbf{m}'}  &\otherwise \\
      \multicolumn{2}{l}{\where \mathbf{m}' = \mathbf{m} \exc (\mathbf{m}'\subb{n}_\pg¬ram)\subrange{o}{z} = \mem\subrange{s}{z}}
    \end{cases} \\
    g &\equiv \CgasOconst + \fnmemgas(\CgasOlinear, z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_Z(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{pages} = 13} &
  $\begin{aligned}
    \using \sq{n, p, c, r} &= \registers\subrange{7}{4} \\
    \using \mathbf{u} &= \begin{cases}
      \mathbf{m}\subb{n}_\pg¬ram &\when n \in \keys{\mathbf{m}} \\
      \error &\otherwise\\
    \end{cases} \\
    \using \mathbf{u}' &= \mathbf{u} \exc \begin{cases}
      (\mathbf{u}'_\ram¬value)_{p\Cpvmpagesize\dots+c\Cpvmpagesize} = \begin{cases}
       \sq{0, 0, \dots} &\when r < 3 \\
        (\mathbf{u}_\ram¬value)_{p\Cpvmpagesize\dots+c\Cpvmpagesize} &\otherwise
      \end{cases} \\
      (\mathbf{u}'_\ram¬access)\subrange{p}{c} = \begin{cases}
        \sq{\none, \none, \dots} &\when r = 0 \\
        \sq{\mathrm{R}, \mathrm{R}, \dots} &\when r \in \set{1, 3} \\
        \sq{\mathrm{W}, \mathrm{W}, \dots} &\when r \in \set{2, 4} \\
      \end{cases}
    \end{cases}\\
    \tup{\registers'_7, \mathbf{m}'} &\equiv \begin{cases}
      \tup{\mathtt{WHO}, \mathbf{m}} &\when \mathbf{u} = \error \\
      \tup{\mathtt{HUH}, \mathbf{m}} &\otherwhen r > 4 \vee p < 16 \vee p+c \ge \nicefrac{2^{32}}{\Cpvmpagesize} \\
      \tup{\mathtt{HUH}, \mathbf{m}} &\otherwhen r > 2 \wedge (\mathbf{u}_\ram¬access)\subrange{p}{c} \ni \none \\
      \tup{\mathtt{OK}, \mathbf{m}'} &\otherwise\,,\ \where \mathbf{m}' = \mathbf{m} \exc \mathbf{m}'\subb{n}_\pg¬ram = \mathbf{u}' \\
    \end{cases} \\
    g &\equiv \begin{cases}
      \CgasZfreeconst + c \cdot \CgasZfreelinear &\when r = 0 \\
      \CgasZallocconst + c \cdot \CgasZalloclinear &\when r \in \set{1, 2} \\
      \CgasZsetmodeconst + c \cdot \CgasZsetmodelinear &\when r \in \set{3, 4} \\
      \CgasZinvalid &\otherwise
    \end{cases}
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_K(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{invoke} = 14} &
  $\begin{aligned}
    \using \sq{n, o} &= \registers_{7, 8} \\
    \using \tup{g_R, \mathbf{w}} &= \begin{cases}
      \tup{g_R, \mathbf{w}}: \encode[8]{g_R} \concat \encode[8]{\mathbf{w}} = \mem\subrange{o}{112} &\when \Nrange{o}{112} \subseteq \writable{\mem} \\
      %\tup{\decode[8]{\memr\subrange{o}{8}}, \sq{\build{\decode[4]{\memr\subrange{o+8+8x}{8}}}{x \orderedin \Nmax{13}}}} &\when \Nrange{o}{60} \subset \writable_\mem} \\
      \tup{0, \error} &\otherwise
    \end{cases} \\
    \using \tup{c, i', g_R', \pg¬gaschargedflag', \mathbf{w}', \mathbf{u}'} &= \Psi(\mathbf{m}\subb{n}_\pg¬code, \mathbf{m}\subb{n}_\pg¬pc, g_R, \mathbf{m}\subb{n}_{\pg¬gaschargedflag}, \mathbf{w}, \mathbf{m}\subb{n}_\pg¬ram)\\
    \using \mem^* &= \mem \exc \mem^*\subrange{o}{112} = \encode[8]{g_R'} \concat \encode[8]{\mathbf{w}'}\\
    \using \mathbf{m}^* &= \mathbf{m} \exc \begin{cases}
      \mathbf{m}^*\subb{n}_\pg¬ram = \mathbf{u}'\\
      \mathbf{m}^*\subb{n}_\pg¬pc = \begin{cases}
        i' + \Fskip(i') + 1 &\when c \in \set{ \host } \times \pvmreg\\
        i' &\otherwise
      \end{cases}\\
      \mathbf{m}^*\subb{n}_{\pg¬gaschargedflag} = \pg¬gaschargedflag'\\
    \end{cases}\\
    \tup{\execst', \registers'_7, \registers'_8, \mem', \mathbf{m}'} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \registers_8, \mem, \mathbf{m}} &\when \mathbf{w} = \error \\
      \tup{\continue, \mathtt{WHO}, \registers_8, \mem, \mathbf{m}} &\otherwhen n \not\in \keys{\mathbf{m}} \\
      \tup{\continue, \mathtt{HOST}, h, \mem^*, \mathbf{m}^*} &\otherwhen c = \host \times h \\
      \tup{\continue, \mathtt{FAULT}, x, \mem^*, \mathbf{m}^*} &\otherwhen c = \fault \times x \\
      \tup{\continue, \mathtt{OOG}, \registers_8, \mem^*, \mathbf{m}^*} &\otherwhen c = \oog \\
      \tup{\continue, \mathtt{PANIC}, \registers_8, \mem^*, \mathbf{m}^*} &\otherwhen c = \panic \\
      \tup{\continue, \mathtt{HALT}, \registers_8, \mem^*, \mathbf{m}^*} &\otherwhen c = \halt \\
    \end{cases} \\
    g &\equiv \CgasK + g_R \\
    \gascounter^* &\equiv \max(\gascounter - g, 0) + \begin{cases}
      0 &\when \mathbf{w} = \error \vee n \not\in \keys{\mathbf{m}} \vee \gascounter < g \\
      g_R' &\otherwise
    \end{cases}
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_X(\gascounter, \registers, \memory, \tup{\mathbf{m}, \pvm¬module¬map, \mathbf{e}})$ \\
  \texttt{expunge} = 15} &
  $\begin{aligned}
    \using \sq{m, n} &= \registers_{7, 8} \\
    \tup{\registers'_7, \mathbf{m}', \pvm¬module¬map'} &\equiv \begin{cases}
      \tup{\mathtt{WHO}, \mathbf{m}, \pvm¬module¬map} &\when (m = 0 \wedge n \not\in \keys{\mathbf{m}}) \vee (m = 1 \wedge n \not\in \keys{\pvm¬module¬map}) \\
      \tup{\mathbf{m}\subb{n}_\pg¬pc, \mathbf{m} \setminus \set{n}, \pvm¬module¬map} &\otherwhen m = 0 \\
      \tup{\mathtt{OK}, \mathbf{m}, \pvm¬module¬map \setminus \set{n}} &\otherwhen m = 1 \\
      \tup{\mathtt{HUH}, \mathbf{m}, \pvm¬module¬map} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasX
  \end{aligned}$\\
  \bottomrule
\end{longtable}

\subsubsection{Accumulate Functions}\label{sec:accumulatefunctions}

These assume a context $\imXY \in \implications^2$; see equation \ref{eq:implications}.
\begin{equation}
  \context \equiv \imXY\ ,\qquad \context' \equiv \tup{\imX', \imY'}
\end{equation}

\newcommand*{\local¬desiredid}{i}

\begin{longtable}{p{3cm} p{13cm}}
  \toprule
  \thead*{\textbf{Function} \\ \textbf{Identifier}} &
  \thead{\textbf{Mutations} \\ \textbf{Gas usage (}$g$\textbf{)}} \\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \endhead
  \makecell*[l]{
  $\Omega_L(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{lookup} = 3} &
  $\begin{aligned}
    \using \mathbf{a} &= \begin{cases}
      \imX_\im¬self &\when \registers_7 = 2^{64} - 1 \\
      (\imX_\im¬state)_\ps¬accounts[\registers_7] &\otherwise
    \end{cases} \\
    \using \sq{h, o} &= \registers\subrange{8}{2} \\
    \using z &= \registers_{11} \\
    \using \mathbf{v} &= \begin{cases}
      \error &\when \Nrange{h}{32} \not\subseteq \readable{\memory} \\
      \none &\otherwhen \mathbf{a} = \none \\
      \mathbf{a}_\sa¬preimages[\memory\subrange{h}{32}] &\otherwise \\
    \end{cases} \\
    \using f &= \min(\registers_{10}, \len{\mathbf{v}}) \\
    \using l &= \min(z, \len{\mathbf{v}} - f) \\
    \tup{\execst', \registers'_7, \memory'\subrange{o}{l}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \memory\subrange{o}{l}} &\when \mathbf{v} = \error \vee \Nrange{o}{l} \not\subseteq \writable{\memory}\\
      \tup{\continue, \mathtt{NONE}, \memory\subrange{o}{l}} &\otherwhen \mathbf{v} = \none \\
      \tup{\continue, \len{\mathbf{v}}, \mathbf{v}\subrange{f}{l}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasLconst + \fnmemgas(\CgasLlinear, z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_R(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{read} = 4} &
  $\begin{aligned}
    \using \mathbf{a} &= \begin{cases}
      \imX_\im¬self &\when \registers_7 = 2^{64} - 1 \\
      (\imX_\im¬state)_\ps¬accounts[\registers_7] &\otherwise
    \end{cases} \\
    \using \sq{k_O, k_Z, o} &= \registers\subrange{8}{3} \\
    \using v_Z &= \registers_{12} \\
    \using \mathbf{v} &= \begin{cases}
      \error &\when \Nrange{k_O}{k_Z} \not\subseteq \readable{\memory} \\
      \none &\otherwhen \mathbf{a} = \none \\
      \mathbf{a}_\sa¬storage\subb{\memory\subrange{k_O}{k_Z}} &\otherwise
    \end{cases} \\
    \using f &= \min(\registers_{11}, \len{\mathbf{v}}) \\
    \using l &= \min(v_Z, \len{\mathbf{v}} - f) \\
    \tup{\execst', \registers'_7, \memory'\subrange{o}{l}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \memory\subrange{o}{l}} &\when \mathbf{v} = \error \vee \Nrange{o}{l} \not\subseteq \writable{\memory}\\
      \tup{\continue, \mathtt{NONE}, \memory\subrange{o}{l}} &\otherwhen \mathbf{v} = \none \\
      \tup{\continue, \len{\mathbf{v}}, \mathbf{v}\subrange{f}{l}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasRconst + \fnmemgas(\CgasRkeylinear, k_Z) + \fnmemgas(\CgasRvallinear, v_Z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_W(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{write} = 5} &
  $\begin{aligned}
    \using d &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \sq{k_O, k_Z, v_O, v_Z} &= \registers\subrange{8}{4} \\
    \using \mathbf{k} &= \begin{cases}
      \memory\subrange{k_O}{k_Z} &\when \Nrange{k_O}{k_Z} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{v} &= \begin{cases}
      \memory\subrange{v_O}{v_Z} &\when \Nrange{v_O}{v_Z} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \begin{cases}
      \mathbf{k} \not\in \keys{\mathbf{d}'\subb{d}_\sa¬storage} & \when \len{\mathbf{v}} = 0 \\
      \mathbf{d}'\subb{d}_\sa¬storage\subb{\mathbf{k}} = \mathbf{v} &\otherwise
    \end{cases} \\
    \using l &= \begin{cases}
      \len{\mathbf{d}\subb{d}_\sa¬storage\subb{\mathbf{k}}} &\when \mathbf{k} \in \keys{\mathbf{d}\subb{d}_\sa¬storage} \\
      \mathtt{NONE} &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX_\im¬state')_\ps¬accounts, \imX_\im¬modifiedaccounts'} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when \mathbf{k} = \error \vee \mathbf{v} = \error\\
      \tup{\continue, \mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen d \not\in \keys{\mathbf{d}} \\
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id \not\in \set{d, \effectivesupervisor{\mathbf{d}}{d}} \\
      \tup{\continue, \mathtt{FULL}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \mathbf{d}'\subb{d}_\sa¬minbalance > \mathbf{d}'\subb{d}_\sa¬balance + \mathbf{d}'\subb{d}_\sa¬supervisorbalance \\
      \tup{\continue, l, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d}} &\otherwise\\
    \end{cases} \\
    g &\equiv \CgasWconst + \fnmemgas(\CgasWkeylinear, k_Z) + \fnmemgas(\CgasWvallinear, v_Z)
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_I(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{info} = 6} &
  $\begin{aligned}
    \using \mathbf{a} &= \begin{cases}
      \imX_\im¬self &\when \registers_7 = 2^{64} - 1 \\
      (\imX_\im¬state)_\ps¬accounts\subb{\registers_7} &\otherwise
    \end{cases} \\
    \using o &= \registers_8 \\
    \using \mathbf{v} &= \begin{cases}
      \begin{aligned}
        &\fnencode(
          \mathbf{a}_\sa¬codehash,
          \encode[4]{\mathbf{a}_\sa¬supervisor, \mathbf{a}_\sa¬supervisorcreated},
          \encode[8]{\mathbf{a}_\sa¬balance, \mathbf{a}_\sa¬supervisorbalance, \mathbf{a}_\sa¬minbalance, \mathbf{a}_\sa¬minaccgas, \mathbf{a}_\sa¬minmemogas, \mathbf{a}_\sa¬octets}, \\
        &\phantom{\fnencode(}
          \encode[4]{\mathbf{a}_\sa¬items},
          \encode[8]{\mathbf{a}_\sa¬gratis},
          \encode[4]{\mathbf{a}_\sa¬created, \mathbf{a}_\sa¬lastacc, \mathbf{a}_\sa¬parent})
      \end{aligned} &\when \mathbf{a} \ne \none \\
      \none &\otherwise
    \end{cases} \\
    \using f &= \min(\registers_{9}, \len{\mathbf{v}}) \\
    \using l &= \min(\registers_{10}, \len{\mathbf{v}} - f) \\
    \tup{\execst', \registers'_7, \memory'\subrange{o}{l}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \memory\subrange{o}{l}} &\when \Nrange{o}{l} \not\subseteq \writable{\memory}\\
      \tup{\continue, \mathtt{NONE}, \memory\subrange{o}{l}} &\otherwhen \mathbf{v} = \none \\
      \tup{\continue, \len{\mathbf{v}}, \mathbf{v}\subrange{f}{l}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasI
  \end{aligned}$ \\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_B(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{bless} = 16} &
  $\begin{aligned}
    \using \sq{m, a, v, r, o, n} &= \registers\subrange{7}{6} \\
    \using \mathbf{a} &= \begin{cases}
      \decode[4]{\memory\subrange{a}{4\Ccorecount}} &\when \Nrange{a}{4\Ccorecount} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{z} &= \begin{cases}
      \set{\build{\kv{s}{g} \ \where \encode[4]{s} \concat \encode[8]{g} = \memory\subrange{o+12i}{12}}{i \in \Nmax{n}}} &\when \Nrange{o}{12n} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_{\tup{\ps¬manager, \ps¬assigners, \ps¬delegator, \ps¬registrar, \ps¬alwaysaccers}}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, (\imX_\im¬state)_{\tup{\ps¬manager, \ps¬assigners, \ps¬delegator, \ps¬registrar, \ps¬alwaysaccers}}} &\when \set{\mathbf{z}, \mathbf{a}} \ni \error \\
      \tup{\continue, \mathtt{HUH}, (\imX_\im¬state)_{\tup{\ps¬manager, \ps¬assigners, \ps¬delegator, \ps¬registrar, \ps¬alwaysaccers}}} &\otherwhen \imX_\im¬id \ne (\imX_\im¬state)_\ps¬manager \\
      \tup{\continue, \mathtt{WHO}, (\imX_\im¬state)_{\tup{\ps¬manager, \ps¬assigners, \ps¬delegator, \ps¬registrar, \ps¬alwaysaccers}}} &\otherwhen \tup{m, v, r} \not\in \serviceid^3 \\
      \tup{\continue, \mathtt{OK}, \tuple{m, \mathbf{a}, v, r, \mathbf{z}}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasBconst + n \cdot \CgasBlinear
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_A(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{assign} = 17} &
  $\begin{aligned}
    \using \sq{c, o, a} &= \registers\subrange{7}{3} \\
    \using \mathbf{q} &= \begin{cases}
      \sq{\build{\memory\subrange{o + 32i}{32}}{i \orderedin \N_\Cauthqueuesize}} &\when \Nrange{o}{32\Cauthqueuesize} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_\ps¬authqueue\subb{c}, (\imX'_\im¬state)_\ps¬assigners\subb{c}} &\equiv \begin{cases}
      \tup{\panic, \registers_7, (\imX_\im¬state)_\ps¬authqueue\subb{c}, (\imX_\im¬state)_\ps¬assigners\subb{c}} &\when \mathbf{q} = \error \\
      \tup{\continue, \mathtt{CORE}, (\imX_\im¬state)_\ps¬authqueue\subb{c}, (\imX_\im¬state)_\ps¬assigners\subb{c}} &\otherwhen c \ge \Ccorecount \\
      \tup{\continue, \mathtt{HUH}, (\imX_\im¬state)_\ps¬authqueue\subb{c}, (\imX_\im¬state)_\ps¬assigners\subb{c}} &\otherwhen \imX_\im¬id \ne (\imX_\im¬state)_\ps¬assigners\subb{c}\\
      \tup{\continue, \mathtt{WHO}, (\imX_\im¬state)_\ps¬authqueue\subb{c}, (\imX_\im¬state)_\ps¬assigners\subb{c}} &\otherwhen a \not\in \serviceid \\
      \tup{\continue, \mathtt{OK}, \mathbf{q}, a} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasA
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_D(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{designate} = 18} &
  $\begin{aligned}
    \using \sq{o, z} &= \registers\subrange{7}{2} \\
    \using \mathbf{v} &= \begin{cases}
      \sq{\build{\memory\subrange{o + 336i}{336}}{i \orderedin \N_z}} &\when \Nrange{o}{336z} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_\ps¬stagingset} &\equiv \begin{cases}
      \tup{\panic, \registers_7, (\imX_\im¬state)_\ps¬stagingset} &\when \mathbf{v} = \error\\
      \tup{\continue, \mathtt{HUH}, (\imX_\im¬state)_\ps¬stagingset} &\otherwhen z \not\in \valcount \vee \imX_\im¬id \ne (\imX_\im¬state)_\ps¬delegator\\
      \tup{\continue, \mathtt{OK}, \mathbf{v}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasDconst + z \cdot \CgasDlinear
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_C(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{checkpoint} = 19} &
  $\begin{aligned}
    \imY' &\equiv \imX \\
    \registers'_7 &\equiv \gascounter^* \\
    g &\equiv \CgasC
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_N(\gascounter, \registers, \memory, \imXY, t)$ \\
  \texttt{new} = 20} &
  $\begin{aligned}
    \using s &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \sq{\local¬desiredid, x, o} &= \registers\subrange{8}{3} \\
    \using &\encode{\sa¬codehash, \encode[4]{l}, \encode[8]{\sa¬minaccgas, \sa¬minmemogas, \sa¬gratis}} = \memory\subrange{o}{60} \\
    \using \tup{i', i^*} &= \begin{cases}
      \tup{\local¬desiredid, \imX_\im¬nextfreeid} &\when \imX_\im¬id = (\imX_\im¬state)_\ps¬registrar \wedge \local¬desiredid < \Cminpublicindex \\
      \tup{\imX_\im¬nextfreeid, \text{check}(\Cminpublicindex + (\imX_\im¬nextfreeid - \Cminpublicindex + 42) \bmod (2^{32} - \Cminpublicindex - 2^8))} &\otherwise
    \end{cases}\\
    \using \mathbf{a} \in \serviceaccount &= \tup{\begin{aligned}
      &\sa¬codehash,
      \is{\mathbf{\sa¬storage}}{\emset},
      \is{\mathbf{\sa¬requests}}{\set{\kv{\tup{\sa¬codehash, l}}{\sq{}}}},
      \sa¬balance,
      \is{\sa¬supervisor}{\imX_\im¬id},
      \is{\sa¬supervisorcreated}{(\imX_\im¬self)_\sa¬created},
      \is{\sa¬supervisorbalance}{\mathbf{a}_\sa¬minbalance - \sa¬balance}, \\
      &\sa¬minaccgas,
      \sa¬minmemogas,
      \is{\mathbf{\sa¬preimages}}{\emset},
      \is{\sa¬created}{t},
      \sa¬gratis,
      \is{\sa¬lastacc}{0},
      \is{\sa¬parent}{\imX_\im¬id}
    \end{aligned}} \\
    &\phantom{{}={}} \where \sa¬balance = \begin{cases}
      \mathbf{a}_\sa¬minbalance &\when x \in \set{0, 1} \\
      0 &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \begin{cases}
      \mathbf{d}'\subb{i'} = \mathbf{a} &\text{always} \\
      \mathbf{d}'\subb{s}_\sa¬balance = \mathbf{d}\subb{s}_\sa¬balance - \mathbf{a}_\sa¬minbalance &\when x \in \set{0, 2} \\
      \mathbf{d}'\subb{s}_\sa¬supervisorbalance = \mathbf{d}\subb{s}_\sa¬supervisorbalance - \mathbf{a}_\sa¬minbalance &\otherwise
    \end{cases} \\
    \using p &= \begin{cases}
      \imX_\im¬id \in \set{s, \effectivesupervisor{\mathbf{d}}{s}} &\when x \in \set{0, 2} \\
      \imX_\im¬id = \effectivesupervisor{\mathbf{d}}{s} &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, \imX'_\im¬nextfreeid, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when \Nrange{o}{60} \not\subseteq \readable{\memory} \\
      \tup{\continue, \mathtt{HUH}, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen l \not\in \preimagelen \\
      \tup{\continue, \mathtt{WHO}, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen s \not\in \keys{\mathbf{d}} \\
      \tup{\continue, \mathtt{HUH}, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen x > 3 \lor \neg p \lor (\sa¬gratis \ne 0 \wedge \imX_\im¬id \ne (\imX_\im¬state)_\ps¬manager) \\
      \tup{\continue, \mathtt{CASH}, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \mathbf{d}'\subb{s}_\sa¬balance < 0 \lor \mathbf{d}'\subb{s}_\sa¬supervisorbalance < 0 \lor {} \\
      &\phantom{\otherwhen} \mathbf{d}'\subb{s}_\sa¬balance + \mathbf{d}'\subb{s}_\sa¬supervisorbalance < \mathbf{d}'\subb{s}_\sa¬minbalance \\
      \tup{\continue, \mathtt{FULL}, \imX_\im¬nextfreeid, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id = (\imX_\im¬state)_\ps¬registrar \wedge \local¬desiredid < \Cminpublicindex \wedge \local¬desiredid \in \keys{\mathbf{d}} \\
      \tup{\continue, i', i^*, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{s}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasN
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_V(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{supervisor} = 29} &
  $\begin{aligned}
    \using d &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using u &= \begin{cases}
      \imX_\im¬id &\when \registers_8 = 2^{64} - 1 \\
      \registers_8 &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \abracegroup{
      \mathbf{d}'\subb{d}_\sa¬supervisor &= u \\
      \mathbf{d}'\subb{d}_\sa¬supervisorcreated &= \mathbf{d}\subb{u}_\sa¬created
    } \\
    \tup{\registers'_7, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when d \not\in \keys{\mathbf{d}} \lor u \not\in \keys{\mathbf{d}} \\
      \tup{\mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id \ne \effectivesupervisor{\mathbf{d}}{d} \\
      \tup{\mathtt{OK}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasV
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_U(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{upgrade} = 21} &
  $\begin{aligned}
    \using d &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \sq{o, a, m} &= \registers\subrange{8}{3} \\
    \using c &= \begin{cases}
      \memory\subrange{o}{32} &\when \Nrange{o}{32} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \abracegroup{
      \mathbf{d}'\subb{d}_\sa¬codehash &= c \\
      \mathbf{d}'\subb{d}_\sa¬minaccgas &= a \\
      \mathbf{d}'\subb{d}_\sa¬minmemogas &= m
    } \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when c = \error \\
      \tup{\continue, \mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen d \not\in \keys{\mathbf{d}} \\
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id \not\in \set{d, \effectivesupervisor{\mathbf{d}}{d}} \\
      \tup{\continue, \mathtt{OK}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasU
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_T(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{transfer} = 22} &
  $\begin{aligned}
    \using \dx¬source &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \dx¬dest &= \begin{cases}
      \imX_\im¬id &\when \registers_8 = 2^{64} - 1 \\
      \registers_8 &\otherwise
    \end{cases} \\
    \using \sq{x, \dx¬amount, l, o} &= \registers\subrange{9}{4} \\
    \using \mathbf{t} \in \sequence{\defxfer} \cup \set{\error} &= \begin{cases}
      \sq{} &\when o = 0 \\
      \sq{\tup{
        \dx¬source,
        \dx¬dest,
        \is{\dx¬destsupervisor}{x \in \set{2, 3}},
        \dx¬amount,
        \is{\dx¬memo}{\memory\subrange{o}{\Cmemosize}},
        \is{\dx¬gas}{l}
      }} &\otherwhen \Nrange{o}{\Cmemosize} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \begin{cases}
      \mathbf{d}'\subb{\dx¬source}_\sa¬balance = \mathbf{d}\subb{\dx¬source}_\sa¬balance - \dx¬amount &\when x \in \set{0, 2} \\
      \mathbf{d}'\subb{\dx¬source}_\sa¬supervisorbalance = \mathbf{d}\subb{\dx¬source}_\sa¬supervisorbalance - \dx¬amount &\when x \in \set{1, 3} \\
      \mathbf{d}'\subb{\dx¬dest}_\sa¬balance = \mathbf{d}\subb{\dx¬dest}_\sa¬balance + \dx¬amount &\when o = 0 \land x \in \set{0, 1} \\
      \mathbf{d}'\subb{\dx¬dest}_\sa¬supervisorbalance = \mathbf{d}\subb{\dx¬dest}_\sa¬supervisorbalance + \dx¬amount &\when o = 0 \land x \in \set{2, 3}
    \end{cases} \\
    \using \mathbf{w} &= \begin{cases}
      \set{\dx¬source, \dx¬dest} &\when o = 0 \\
      \set{\dx¬source} &\otherwise
    \end{cases} \\
    \using p_s &= \begin{cases}
      \imX_\im¬id \in \set{\dx¬source, \effectivesupervisor{\mathbf{d}}{\dx¬source}} &\when x \in \set{0, 2} \\
      \imX_\im¬id = \effectivesupervisor{\mathbf{d}}{\dx¬source} &\otherwise
    \end{cases} \\
    \using p_d &= o \ne 0 \lor (\imX_\im¬id \in \set{\dx¬dest, \effectivesupervisor{\mathbf{d}}{\dx¬dest}} \land (x \in \set{1, 2} \lor \dx¬source \ne \dx¬dest)) \\
    \using \tup{c, t} &= \begin{cases}
      \tup{\panic, 0} &\when \mathbf{t} = \error \\
      \tup{\mathtt{WHO}, 0} &\otherwhen \dx¬source \not\in \keys{\mathbf{d}} \lor \dx¬dest \not\in \keys{\mathbf{d}} \\
      \tup{\mathtt{HUH}, 0} &\otherwhen x > 3 \lor \neg p_s \lor \neg p_d \\
      \tup{\mathtt{LOW}, 0} &\otherwhen o \ne 0 \land l < \mathbf{d}\subb{\dx¬dest}_\sa¬minmemogas \\
      \tup{\mathtt{CASH}, 0} &\otherwhen \mathbf{d}'\subb{\dx¬source}_\sa¬balance < 0 \lor \mathbf{d}'\subb{\dx¬source}_\sa¬supervisorbalance < 0 \lor \mathbf{d}'\subb{\dx¬source}_\sa¬balance + \mathbf{d}'\subb{\dx¬source}_\sa¬supervisorbalance < \mathbf{d}'\subb{\dx¬source}_\sa¬minbalance \\
      \tup{\mathtt{OK}, 0} &\otherwhen o = 0 \\
      \tup{\mathtt{OK}, l} &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, \imX'_\im¬xfers, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \imX_\im¬xfers, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when c = \panic \\
      \tup{\continue, c, \imX_\im¬xfers, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen c \ne \mathtt{OK} \\
      \tup{\continue, \mathtt{OK}, \imX_\im¬xfers \concat \mathbf{t}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \mathbf{w}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasT + t
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_J(\gascounter, \registers, \memory, \imXY, t)$ \\
  \texttt{eject} = 23} &
  $\begin{aligned}
    \using d &= \registers_7 \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \mathbf{d} \exc \abracegroup{
      &d \not\in \keys{\mathbf{d}'} \\
      &\mathbf{d}'\subb{\imX_\im¬id}_\sa¬balance = (\imX_\im¬self)_\sa¬balance + \mathbf{d}\subb{d}_\sa¬balance + \mathbf{d}\subb{d}_\sa¬supervisorbalance \\
    } \\
    \tup{\registers'_7, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when d \not\in \keys{\mathbf{d}} \\
      \tup{\mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen d = \imX_\im¬id \vee \imX_\im¬id \ne \effectivesupervisor{\mathbf{d}}{d} \vee {} \\
      &\phantom{\otherwhen} \mathbf{d}\subb{d}_\sa¬created = t \vee \mathbf{d}\subb{d}_\sa¬items \ne 0 \\
      \tup{\mathtt{OK}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d, \imX_\im¬id}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasJ
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_Q(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{query} = 24} &
  $\begin{aligned}
    \using \mathbf{s} &= \begin{cases}
      \imX_\im¬self &\when \registers_7 = 2^{64} - 1 \\
      (\imX_\im¬state)_\ps¬accounts\subb{\registers_7} &\otherwise
    \end{cases} \\
    \using \sq{o, z} &= \registers_{8, 9} \\
    \using h &= \begin{cases}
      \memory\subrange{o}{32} &\when \Nrange{o}{32} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{a} &= \mathbf{s}_\sa¬requests\subb{\tup{h, z}} \\
    \tup{\execst', \registers'_7, \registers'_8} &\equiv \begin{cases}
      \tup{\continue, \mathtt{HUH}, \registers_8} &\when z \not\in \preimagelen \\
      \tup{\panic, \registers_7, \registers_8} &\otherwhen h = \error \\
      \tup{\continue, \mathtt{WHO}, \registers_8} &\otherwhen \mathbf{s} = \none \\
      \tup{\continue, \mathtt{NONE}, 0} &\otherwhen \mathbf{a} = \none \\
      \tup{\continue, 0, 0} &\otherwhen \mathbf{a} = \sq{} \\
      \tup{\continue, 1 + 2^{32}x, 0} &\otherwhen \mathbf{a} = \sq{x} \\
      \tup{\continue, 2 + 2^{32}x, y} &\otherwhen \mathbf{a} = \sq{x, y} \\
      \tup{\continue, 3 + 2^{32}x, y + 2^{32}z} &\otherwhen \mathbf{a} = \sq{x, y, z} \\
    \end{cases} \\
    g &\equiv \CgasQ
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_S(\gascounter, \registers, \memory, \imXY, t)$ \\
  \texttt{solicit} = 25} &
  $\begin{aligned}
    \using d &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \sq{o, z} &= \registers_{8, 9} \\
    \using h &= \begin{cases}
      \memory\subrange{o}{32} &\when \Nrange{o}{32} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \begin{cases}
      \mathbf{d} \text{ except: } &\\
      \quad \mathbf{d}'\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{} &\when \tup{h, z} \not\in \keys{\mathbf{d}\subb{d}_\sa¬requests} \\
      \quad \mathbf{d}'\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x, y, t} &\when \mathbf{d}\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x, y} \\
      \error &\otherwise\\
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when z \not \in \preimagelen \\
      \tup{\panic, \registers_7, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen h = \error \\
      \tup{\continue, \mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen d \not\in \keys{\mathbf{d}} \\
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id \not\in \set{d, \effectivesupervisor{\mathbf{d}}{d}} \lor \mathbf{d}' = \error \\
      \tup{\continue, \mathtt{FULL}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \mathbf{d}'\subb{d}_\sa¬balance + \mathbf{d}'\subb{d}_\sa¬supervisorbalance < \mathbf{d}'\subb{d}_\sa¬minbalance \\
      \tup{\continue, \mathtt{OK}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasS
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_F(\gascounter, \registers, \memory, \imXY, t)$ \\
  \texttt{forget} = 26} &
  $\begin{aligned}
    \using d &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \sq{o, z} &= \registers_{8, 9} \\
    \using h &= \begin{cases}
      \memory\subrange{o}{32} &\when \Nrange{o}{32} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts \\
    \using \mathbf{d}' &= \begin{cases}
      \mathbf{d} \text{ except:} &\\
      \quad \tup{h, z} \not\in \keys{\mathbf{d}'\subb{d}_\sa¬requests} &\when \mathbf{d}\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{} \\
      \quad \left.
        \begin{aligned}
          \tup{h, z} &\not\in \keys{\mathbf{d}'\subb{d}_\sa¬requests}\ ,\\[2pt]
          h &\not\in \keys{\mathbf{d}'\subb{d}_\sa¬preimages}
        \end{aligned}
      \ \right\} &\when \mathbf{d}\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x, y},\ y < t - \Cexpungeperiod \\
      \quad \mathbf{d}'\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x, t} &\when \mathbf{d}\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x} \\
      \quad \mathbf{d}'\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{w, t} &\when \mathbf{d}\subb{d}_\sa¬requests\subb{\tup{h, z}} = \sq{x, y, w},\ y < t - \Cexpungeperiod \\
      \error &\otherwise\\
    \end{cases} \\
    \tup{\execst', \registers'_7, (\imX'_\im¬state)_\ps¬accounts, \imX'_\im¬modifiedaccounts} &\equiv \begin{cases}
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\when z \not \in \preimagelen \\
      \tup{\panic, \registers_7, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen h = \error \\
      \tup{\continue, \mathtt{WHO}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen d \not\in \keys{\mathbf{d}} \\
      \tup{\continue, \mathtt{HUH}, \mathbf{d}, \imX_\im¬modifiedaccounts} &\otherwhen \imX_\im¬id \not\in \set{d, \effectivesupervisor{\mathbf{d}}{d}} \lor \mathbf{d}' = \error \\
      \tup{\continue, \mathtt{OK}, \mathbf{d}', \imX_\im¬modifiedaccounts \cup \set{d}} &\otherwise
    \end{cases} \\
    g &\equiv \CgasF
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_\Taurus(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{yield} = 27} &
  $\begin{aligned}
    \using o &= \registers_7 \\
    \using h &= \begin{cases}
      \memory\subrange{o}{32} &\when \Nrange{o}{32} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, \imX'_\im¬yield} &\equiv \begin{cases}
      \tup{\panic, \registers_7, \imX_\im¬yield} &\when h = \error \\
      \tup{\continue, \mathtt{OK}, h} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasTaurus
  \end{aligned}$\\
  \cmidrule(lr){1-1}\cmidrule(lr){2-2}
  \makecell*[l]{
  $\Omega_\Aries(\gascounter, \registers, \memory, \imXY)$ \\
  \texttt{provide} = 28} &
  $\begin{aligned}
    \using \sq{o, z} &= \registers_{8, 9} \\
    \using \mathbf{d} &= (\imX_\im¬state)_\ps¬accounts\\
    \using s &= \begin{cases}
      \imX_\im¬id &\when \registers_7 = 2^{64} - 1 \\
      \registers_7 &\otherwise
    \end{cases} \\
    \using \mathbf{i} &= \begin{cases}
      \memory\subrange{o}{z} &\when \Nrange{o}{z} \subseteq \readable{\memory} \\
      \error &\otherwise
    \end{cases} \\
    \using \mathbf{a} &= \begin{cases}
      \mathbf{d}[s] &\when s \in \keys{\mathbf{d}} \\
      \none &\otherwise
    \end{cases} \\
    \tup{\execst', \registers'_7, \imX'_\im¬provisions} &\equiv \begin{cases}
      \tup{\continue, \mathtt{HUH}, \imX_\im¬provisions} &\when z \not \in \preimagelen \\
      \tup{\panic, \registers_7, \imX_\im¬provisions} &\otherwhen \mathbf{i} = \error \\
      \tup{\continue, \mathtt{WHO}, \imX_\im¬provisions} &\otherwhen \mathbf{a} = \none \\
      \tup{\continue, \mathtt{HUH}, \imX_\im¬provisions} &\otherwhen \mathbf{a}_\sa¬requests[\tup{\blake{\mathbf{i}}, z}] \ne \sq{} \\
      \tup{\continue, \mathtt{HUH}, \imX_\im¬provisions} &\otherwhen \tup{s, \mathbf{i}} \in \imX_\im¬provisions \\
      \tup{\continue, \mathtt{OK}, \imX_\im¬provisions \cup \set{\tup{s, \mathbf{i}}}} &\otherwise \\
    \end{cases} \\
    g &\equiv \CgasAriesconst + \fnmemgas(\CgasArieslinear, z)
  \end{aligned}$\\
  \bottomrule
\end{longtable}

\undef{\local¬desiredid}
